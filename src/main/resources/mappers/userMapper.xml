<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.memo.user.dao.UserDAO">
 <select id="selectUserByLoginId" parameterType="string"
 resultType="com.memo.user.model.User">

	select
	id,
	loginId,
	password,
	name,
	email,
	createdAt,
	updatedAt
	from
	user
	where
	loginId = #{loginId}
 
 
 </select>
 
  <select id="getUserByLoginId" parameterType="map"
 resultType="com.memo.user.model.User">

	select
	id,
	loginId,
	password,
	name,
	email,
	createdAt,
	updatedAt
	from
	user
	where
	loginId = #{loginId}
	and
	password= #{password}
 
 
 </select>
 
 
 <insert id="insertUser" parameterType="map">
	insert into user
	(
	loginId,
	password,
	name,
	email,
	createdAt,
	updatedAt)
	values
	(
	#{loginId},
	#{password},
	#{name},
	#{email},
	now(),
	now()
	)
 
 
 </insert>
 
 
 </mapper>